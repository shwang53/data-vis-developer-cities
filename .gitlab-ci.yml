pages:
  image: node:6
  stage: deploy
  script:
  - npm install
  - npm run-script build
  - mkdir .public
  - cp -r * .public
  - mv .public public
  artifacts:
    paths:
    - public
  only:
  - master
